import 'package:flutter/material.dart';

class TetrisScreen extends StatelessWidget {
  final String title;
  final Color backgroundColor;

  const TetrisScreen(this.title, this.backgroundColor, {Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
